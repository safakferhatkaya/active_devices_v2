<% unless @device.is_credentials_showed %>
  swal("Credentials", "Reference <%= @device.reference %>\nPassword <%= @device.password %>", "success");
<% else %>
  swal("You already seen this", "", "error");  
<% end %>